# ── Web framework ──────────────────────────────────────────────
fastapi==0.115.12
uvicorn[standard]==0.34.3
python-multipart==0.0.20

# ── Data validation ────────────────────────────────────────────
# bumped to >=2.12.5 to satisfy hermes-agent + exa-py requirements
pydantic==2.12.5
pydantic-settings==2.9.1

# ── Barcode decoding ────────────────────────────────────────────
# zxing-cpp provides Python bindings for the ZXing-C++ library.
# pyzbar is the zbar Python wrapper (requires libzbar0 system package).
zxing-cpp==2.3.0
pyzbar==0.1.9

# ── Image processing ───────────────────────────────────────────
Pillow==11.2.1
# opencv-python-headless (no GUI deps) — opencv-python 4.13 requires numpy>=2
opencv-python-headless==4.11.0.86
# numpy>=2 required by opencv-python 4.13+ on Python 3.9+
numpy==2.2.6
scipy==1.15.3

# ── PDF417 / AAMVA parsing ──────────────────────────────────────
# aamva-barcode-library has no Python 3.12+ wheel on PyPI.
# The parser (app/core/barcode/parser.py) contains a full hand-rolled
# fallback used automatically when the library is absent — no loss of
# functionality. Uncomment below only if running Python <=3.11:
# aamva-barcode-library==0.3.0

# ── OCR — Sprint 2 (uncomment when ready) ────────────────────────
# paddleocr==2.7.3
# paddlepaddle==2.6.1
# Install separately: pip install paddlepaddle paddleocr

# ── Utilities ──────────────────────────────────────────────────
# bumped to >=1.2.1 to satisfy hermes-agent requirement
python-dotenv==1.1.0
structlog==25.4.0
ulid-py==1.1.0
# bumped to >=0.28.1 to satisfy hermes-agent + exa-py requirements
httpx[socks]==0.28.1
python-dateutil==2.9.0
usaddress==0.5.10

# ── Testing ────────────────────────────────────────────────────
pytest==8.3.5
pytest-asyncio==0.26.0
Faker==37.3.0
