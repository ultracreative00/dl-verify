# ── Web framework ──────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# ── Data validation ────────────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.3.0

# ── Barcode decoding ────────────────────────────────────────────
# zxing-cpp provides Python bindings for the ZXing-C++ library.
# pyzbar is the zbar Python wrapper (requires libzbar0 system package).
zxing-cpp==2.2.0
pyzbar==0.1.9

# ── Image processing ───────────────────────────────────────────
Pillow==10.3.0
opencv-python-headless==4.10.0.82
numpy==1.26.4
scipy==1.13.1

# ── PDF417 / AAMVA parsing ──────────────────────────────────────
# aamva-barcode-library has no Python 3.12+ wheel on PyPI.
# The parser (app/core/barcode/parser.py) already contains a full
# hand-rolled fallback that is used automatically when the library
# is absent — no functionality is lost.
#
# If you are on Python <=3.11 and want the library as an optional
# accelerator you may uncomment the line below:
# aamva-barcode-library==0.3.0

# ── OCR — Sprint 2 (comment out if not needed) ─────────────────
# paddleocr==2.7.3
# paddlepaddle==2.6.1
# Install separately: pip install paddlepaddle paddleocr

# ── Utilities ──────────────────────────────────────────────────
python-dotenv==1.0.1
structlog==24.2.0
ulid-py==1.1.0
httpx==0.27.0
python-dateutil==2.9.0
usaddress==0.5.10

# ── Testing ────────────────────────────────────────────────────
pytest==8.2.1
pytest-asyncio==0.23.7
Faker==25.3.0
